import Button from '@mui/joy/Button';

function App() {
  return (
    <div>
      <Button variant="solid">Give</Button>
      <Button variant="soft">Your</Button>
      <Button variant="outlined">Balls</Button>
      <Button variant="plain">a Tug</Button>
    </div>
  );
}

export default App;